const express = require('express');
const router = express.Router();
const { errorHandler } = require('../utils/errorHandler');
const {} = require('../services/public-service');

// NOTICE: Please add public APIS only

// Return router
module.exports = router;
